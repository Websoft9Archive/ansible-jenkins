jdk_version: "8" 
nginx_reverse_proxy_port: "8080"

preend_certbot: True

init_application:
  jenkins:
    username: admin
    password: "123456"
    service_before: jenkins-server
    commands: 
      - rm -rf /var/lib/jenkins/*
      - rm -rf /credentials
